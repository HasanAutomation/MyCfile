#include<stdio.h>
int main(int argc, char const *argv[])
{
    int a,b,sum;
    printf("enter two number");
    scanf("%d%d",&a,&b);
    sum=a+b;
    printf("sum of two nuber is %d",sum);

    return 0;
}
